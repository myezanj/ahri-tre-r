{
  "name": "ahri-tre-r",
  "dockerComposeFile": "docker_compose.yml",
  "service": "r",
  "workspaceFolder": "/workspaces/ahri-tre-r",
  "remoteUser": "vscode",
  "features": {},
  "customizations": {
    "vscode": {
      "extensions": [
        "REditorSupport.r",
        "rdebugger.r-debugger"
      ]
    }
  },
  "postCreateCommand": "R -q -e \"if (!requireNamespace('remotes', quietly = TRUE)) install.packages('remotes', repos = 'https://cloud.r-project.org'); remotes::install_deps('.', dependencies = TRUE, upgrade = 'never'); if (!requireNamespace('roxygen2', quietly = TRUE)) install.packages('roxygen2', repos = 'https://cloud.r-project.org'); roxygen2::roxygenise(); if (file.exists('/opt/ahri-tre-runtime/bin/ahri-tre') && file.exists('/opt/ahri-tre-runtime/bin/ahri-tred')) { if (!requireNamespace('testthat', quietly = TRUE)) install.packages('testthat', repos = 'https://cloud.r-project.org'); testthat::test_dir('tests/testthat') } else { message('AHRI TRE runtime not installed in this container build; skipping tests that require runtime.') }\""
}
